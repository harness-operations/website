{
  "name": "@harness-operations/website",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:release": "node scripts/validate-release.mjs",
    "sync:spec": "node scripts/sync-spec.mjs",
    "dev": "npm run validate:release && npm run sync:spec && astro dev",
    "build": "npm run validate:release && npm run sync:spec && astro build && node scripts/check-built-links.mjs",
    "preview": "astro preview",
    "test:smoke": "playwright test"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "7.2.4",
    "@astrojs/starlight": "0.41.7",
    "astro": "7.2.6"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
